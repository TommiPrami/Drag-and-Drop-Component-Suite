This folder contains the run- and design-time packages source files.

To install:
1) Open the DragAndDropComponentSuite.groupproj project group in Delphi.
2) In the project manager, right-click DragDropRunTimePackage package and select Build.
3) In the project manager, right-click DragDropDesignPackage package and select Install (this automatically builds the package first).
